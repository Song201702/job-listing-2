<div class="container-fluid">


  <h1> <%= @job.title %> </h1>

  <p>
    <%= simple_format(@job.description) %>
  </p>

  <div class="text-center">
      <%= link_to("递交简历", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration: underline;") %>
  </div>
  <p>
    <div class="text-center">
      <%= link_to "全部职位", jobs_path, :style => "font-size: 20px; text-decoration: underline;" %>
    </div>
    <!--有职位 end-->
  </p>
</div>
